# 小游戏品牌概念工坊 · 版本记录

## v2.3.1 — 修复「点生成 loading 被旧图秒铺回」（2026-08-12）
- 修复 bug：点「生成」/「导出 job.json」后，画布本该进入循环 loading 等真图，却被上一套 freerun/套图（如 simjinx）瞬间铺回，loading 一闪就没了。
- 根因：`startPolling` 降级读静态 `outputs/manifest.json` 时，把「出图前就躺在 outputs/ 里的旧图」误判成本批新图，一 tick 就 applyFilled 覆盖了 loading。
- 修法：点生成时记录「出图前」manifest.cells 指纹 `__PREGEN_SIG__` 并置 `__GEN_ACTIVE__=true`；轮询时只要静态 manifest 内容仍等于出图前指纹（或为空），一律忽略、保持 loading；只有内容真正变化（后台写入本批新图）才解除标记、逐张点亮。
- 效果：纯静态线上点生成 = loading 一直循环，直到 agent 后台出新图更新 manifest/freerun 才点亮，符合「缓解等待焦虑」诉求。

## v2.3 — 自由出图展示 + json 自带参考图（2026-08-12）
- 新增「自由出图（不套技能）」结果展示：启动时若存在 `outputs/freerun.json`，直接把这套图铺进画布、右栏用它的真分析（配色/关键词/世界观）。技能 snapshot 依旧物理隔离，freerun 独立不串色。
- 首套自由出图 = 金克丝，锚点用用户上传的真金克丝角色设定图（紫麻花辫/鲨鱼火箭筒/涂鸦街头风），nano pro 图生图 input_fidelity=high，竖版 9:16，角色一致性极高。
- 导出 job.json 现自带参考图：`exportJob` 会把三类参考图（角色/图形/玩法）base64 编码进 `job.ref_images`，agent 读 json 即可直接拿到图，无需另发。
- 「不走技能时 loading 作占位 → 拿到图自动点亮」链路确认：生成即 loading 占位，agent 回填即逐张点亮。

## v2.2 — 横竖版比例严格跟随选择器（2026-08-12）
- 修复「不管选横版竖版，出的图都是横版」的比例失效 bug。根因：出图 prompt 里有 3 处 aspect 被写死，根本没读「游戏尺寸」选择器。
- 主角立绘：原来永远 `9:16` 竖版 → 现严格按选定比例（`ratioLabel(ctx.size)`）。
- 组件页：原来永远 `16:9` 横版 → 现严格按选定比例。
- 关键界面：原来只在句首弱提比例、易被模型忽略 → 现三处统一追加 `Output strictly in {比例} aspect ratio` 强约束，写进 positive prompt。
- 效果：选竖版 = 主角/组件/界面全 9:16；选横版 = 全 16:9，整套比例一致，不再乱套。

## v2.1 — 样图机制：两套技能物理隔离，不选即空（2026-08-12）
- 新增第二套完整样图：金克丝 × 街头二次元可爱风（5 界面 gpt-image-2，薄荷青绿→天蓝渐变 + 荧光黄绿 #CEF40A CTA + 白色贴纸模切描边）。
- 建「样图机制」：每个技能各存一份 snapshot/（manifest + analysis + 整套图），selectSkill 时加载该技能自己的样图+配色分析，两套物理隔离、永不串色。
- 不选技能时画布空、右栏空、配色格空，只留「请先选择风格技能」占位提示（首开即空态，不再默认铺符文之境那套）。
- 关键词分析「应用」标签在 flex 中被长文本拉伸成大灰空框 → 已修（改独立块 + align-self:flex-start），只显示文字。

## v2.0 — 金克丝 × 符文之境魔幻史诗像素风（2026-08-12）
- 主角换金克丝（英雄联盟三视图图生图锁角色，input_fidelity=high 逐张复用）。
- 出图模型选择器：nano pro / image2 / 对比输出（默认对比：同格 nano pro + gpt-image-2 双图）。
- 5 界面完整套图 × 双模型 = 10 张真图（登录/关卡/战斗/结算/组件）。
- 右栏真分析全部按本套 UI 重新提取：配色 DNA 4 组 11 色（深海夜蓝 / 宝石青蓝符文光 #43DAE2 / 秘境苔绿 / 烫金压黑），关键词 3 条，世界观 2 套（符文裂潮 Rune Tide / 军械师的秘境）。
- lightbox 对比双图铺满：点对比格弹出 nano（上）+ gpt（下）两张横版图各自完整 contain 铺满，不再左右硬裁、不再只弹一张。
- 出图时全格进无限循环 loading（流光+呼吸点），后台轮询逐张点亮真图。

## v1.0 — 狄瑞吉 × 造次游戏皮肤 demo（基线）
- 首版三栏可视化编排器 demo，主角狄瑞吉，配色次元紫/玫红/暖金，玩法 UNDEAD RUSH。
- 荧光草绿 neon 皮肤、组员端自跑技能、后端插口契约、组员使用说明入口。
