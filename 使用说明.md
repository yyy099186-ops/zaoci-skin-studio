# StyleForge 设计工作台 · 使用说明

一个「风格技能 → 成套小游戏 UI 方案」的本地设计工具。装了三谦 MCP 的设计组同学，双击即可使用。

---

## 一、怎么启动

**Mac：** 双击 `启动.command`
> 首次打开若被系统拦截，右键 → 打开 → 再点"打开"即可（只需一次）。

**Windows：** 双击 `启动.bat`

启动后会自动打开浏览器，地址是 `http://localhost:8799/index.html`。
关闭工具：直接关掉那个黑色的启动窗口。

> 前提：电脑装了 Python3。没装的话脚本会提示你去 https://www.python.org/downloads/ 下载安装（Windows 安装时记得勾 "Add Python to PATH"）。

---

## 二、怎么用（4 步出一套方案）

### 1. 选风格
左栏「小游戏风格」里选一张技能卡：
- **符文之境像素** —— 深海蓝黑金、史诗魔幻像素风（出图走 nano pro）
- **街头二次元可爱** —— 白底糖果色、萌系贴纸风（出图走 gpt-image-2）

选中后整个界面配色会跟着变——这是工具真读懂了技能的配色 DNA。卡片右上角自动标了该风格该用哪个出图模型，不用你手选。

### 2. 填参数
- **主题词**：比如"冬日极光猫旅""夏日海岛大作战"
- **品牌语/吉祥物/主角**：比如"薄荷绿卫衣探险萌娃"
- **角色比例**：3 / 5 / 7 / 9 头身
- **游戏尺寸**：横版 16:9 / 竖版 9:16

（可选）左上传参考图：给了角色参考图，出图时会**图生图锁定同一个角色**，保证所有界面里是同一个人。

### 3. 点「生成」
中间 6 格会依次出图：
- **配色/材质图 + UI 组件拆分**：设计语言拆解，用技能数据直接渲染
- **登录页 / 关卡页 / 战斗页 / 结算页**：4 张关键界面

第一次没有真图时，会用技能自带样本图或皮肤色占位，并拼好每格的完整 Prompt。

### 4. 点「交 agent 真出图」
这一步是真出图的关键 —— 见下方第三节。

---

## 三、怎么真出图（agent 代跑回填）

**为什么需要这一步？**
浏览器里的网页出于安全限制，没法直接调三谦出图接口。所以工具把「出图任务」交给你自己的 agent（就是你平时在 WorkBuddy 里对话的那个），由它复用你已授权的三谦通道真出图。**零 token 泄露风险，用的是你自己的授权。**

**操作：**
1. 在工具里点「交 agent 真出图」——任务会自动存到工具目录的 `jobs/latest.json`
2. 回到 WorkBuddy 对话，对你的 agent 说：
   > 读取「品牌设计工作流工具/jobs/latest.json」，按里面每格的 prompt / model / aspect 真调三谦出图，登录/关卡/战斗/结算这几张要图生图复用同一个主角保持角色一致，出完把图存进 outputs/ 并更新 outputs/manifest.json
3. agent 出完图、写好 `manifest.json` 后，**工具会自动轮询检测到并把真图贴进 6 格**（无需刷新），点格子可看大图。

> 高峰期（每天 14:30–20:30）nano pro（像素风）可能被 Google 限流失败，避峰或让 agent 重试即可。gpt-image-2（二次元风）不受影响。

---

## 四、怎么加新风格

左栏最下方「+ 上传风格技能包 (.zip)」——丢任何符合这套 schema 的技能 zip 进去，工具会当场解析、长出一张新风格卡、套上新皮肤。这就是工具「能学习进化」的入口。

技能包的标准结构：
```
风格名.zip
├── SKILL.md              风格说明 + 出图铁律
└── references/
    ├── tokens.json       配色/字体/形状 tokens
    ├── style.json        art_direction / negative_prompt / asset_templates
    ├── meta.json         tags / mood / 模型路由
    ├── style-guide.md    风格指南
    └── samples/          （可选）样本图
```

---

## 五、目录说明

```
品牌设计工作流工具/
├── 启动.command / 启动.bat   双击启动
├── server.py                 本地后端（静态服务 + job/outputs 接口）
├── index.html                工具界面
├── skills/                   内置风格技能库
│   ├── runeterra-pixel/
│   └── street-anime-kawaii/
├── jobs/                     点生成后的出图任务（agent 读这里）
├── outputs/                  真图 + manifest.json（agent 写这里，工具读这里）
└── 使用说明.md
```
